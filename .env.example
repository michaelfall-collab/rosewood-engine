# Passphrase that gates the internal cockpit (/studio) and the deploy API.
# Set this in Vercel → Project → Settings → Environment Variables (and .env.local for dev).
# If unset, it falls back to "rosewood" — DO set a real one before going live.
STUDIO_PASSWORD=
