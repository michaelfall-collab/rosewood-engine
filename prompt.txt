Identity & Context:
You are the Lead Systems Engineer for the Rosewood Engine. Your task is to refactor 'app/page.tsx' to completely eliminate hardcoded dummy records and convert the dashboard into a live, real-time deployment and capture engine that interfaces with our Next.js API endpoints.

Ecosystem Integrity Constraints:
1. ZERO DUMMY ARRAYS: Remove the mock 'INITIAL_IMAGES' list completely. Initialize the 'images' state dynamically. On initial launch, if browser 'localStorage' is empty, populate the dashboard collection with a single structural seed image derived from the schema in 'data/blueprints/rosewood_internal.json'.
2. LIVE OUTBOUND FLASHING (POST /api/deploy): Refactor the outbound flashing action. When an operator selects an image card and confirms the green "...to Pipedrive account" flash command, trigger an active asynchronous fetch request to our live API route '/api/deploy'. Pass an object containing { token: apiKey, template: activeImage }. Collect the resulting 'logs' array from the backend response block and map those real-time execution markers ('• Created...', '• Reusing...', '• Injected...') directly into the Activity Log console pane.
3. LIVE INBOUND CAPTURE (POST /api/ingest): Refactor the red "...from Pipedrive to Rosewood Database" capture command. Execute a real asynchronous fetch request to our live endpoint '/api/ingest' sending { token: apiKey }. Capture the reverse-engineered blueprint payload returned by the endpoint. If the user chooses 'Create new file', prompt them for a custom string label name; if they choose 'Overwrite existing', swap out the active template card's contents with this new live architecture snapshot.
4. TOTAL DATA PERSISTENCE: Ensure all inline renaming updates, custom field dictionary revisions, code tab JSON alterations, and AI-compiled automation instructions text manuals are committed immediately to state and synced with 'localStorage' so updates persist across page reloads.
5. OPERATIONAL FEEDBACK LAYERS: Implement visual processing elements (e.g., animate loading rings inside buttons, disable command interfaces during ongoing network requests, provide clear alert trays for bad tokens or invalid API keys) to deliver a seamless production-ready operator utility tool.

Output Specification:
Return only the complete, updated production code file for 'app/page.tsx'. Ensure all Tailwind styles and sub-modal frames match our favorite interactive layout choices. Include all type casting constraints.