================================================================================
🎯 TARGET DESTINATIONS: types/blueprint.ts, app/api/ingest/route.ts, app/api/deploy/route.ts
🧱 LOCAL WORKSPACE FILES TO PIPE: cat types/blueprint.ts app/api/ingest/route.ts app/api/deploy/route.ts prompt.txt | gemini
================================================================================

Identity & Context:
You are the Principal Software Architect for the Rosewood Engine. Your task is to upgrade our type definitions and core API endpoints to ingest and deploy a complete, multi-object database registry including Custom Fields (Deals, Persons, Organizations, Products), Activity Types, and Lost Reasons.

Strict Engineering Specifications:

1. CORE TYPE UPGRADES (types/blueprint.ts):
- Extend the 'CRMArchitectureBlueprint' interface to natively house three new optional array schemas: 'customFields', 'activityTypes', and 'lostReasons'.
- Define a 'CustomFieldSpec' interface containing fields: 'key' (a sanitized string, e.g., 'cf_shed_style'), 'name' (the exact user-facing display label), 'type' (e.g., 'text', 'varchar', 'enum', 'monetary'), 'field_type' (identifying the parent object: 'deal', 'person', 'organization', or 'product'), and an optional 'options' array for dropdown selections.
- Define an 'ActivityTypeSpec' interface with fields: 'name', 'icon_key', and 'color'.
- Define a 'LostReasonSpec' interface with a string 'reason' array property.

2. MULTI-OBJECT INGESTION ROUTE (app/api/ingest/route.ts):
- Update the HTTP POST handler to execute concurrent, parallel fetch streams targeting:
  * /v1/dealFields
  * /v1/personFields
  * /v1/organizationFields
  * /v1/productFields
  * /v1/activityTypes
  * /v1/lostReasons
- Process the incoming metadata streams: filter out all built-in system native fields (where 'edit_flag' or 'custom_field' boolean trackers dictate it is standard) and map only true custom fields into the 'customFields' collection array.
- For each custom field, generate an internal string 'key' identifier by sanitizing and lowercasing the display name (e.g., "Shed Style" becomes "cf_shed_style") to keep our blueprint independent of environment hashes.

3. DYNAMIC TRANSLATION DEPLOYMENT ROUTE (app/api/deploy/route.ts):
- Enhance the loop mechanics in the deployment engine to prevent duplicate records: perform safe name matching lookups before executing creation sequences.
- First Pass - Custom Field Creation: Iterate through 'template.customFields'. Check the target workspace if a field with the exact same name and 'field_type' already exists. If missing, post it to the correct endpoint (e.g., POST /v1/dealFields). 
- Hash Resolution Memory: Capture the new 40-character alphanumeric hash string returned by the Pipedrive API payload and dynamically populate a memory registry tracking dictionary ('fieldKeyTranslationMap') that maps the clean blueprint key to the new remote system hash.
- Subsequent Passes - Activity Types & Lost Reasons: Safely loop through and construct missing 'activityTypes' and 'lostReasons' via their respective native API routes.
- Return Logs: Ensure the 'logs' array captures clear output strings tracking the lifecycle of fields (e.g., "• Injected Custom Deal Field: 'Shed Style' (New Key: abc123xyz...)").

Output Specifications:
Return your response as three separate, production-ready code files wrapped in distinct markdown code blocks corresponding exactly to:
- Block 1: types/blueprint.ts
- Block 2: app/api/ingest/route.ts
- Block 3: app/api/deploy/route.ts

Ensure all type assertions, response formatting blocks, error catches, and interface validations are explicitly written out with no partial omissions or placeholders.